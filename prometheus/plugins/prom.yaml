---
type: "native"
requirements: ["requests==2.18.4"]
interval: 30

settings:
  - name: endpoints
    type: string
    default: "http://localhost:9100/metrics"
    description: Comma-delimited list of URL's to scrape for Prometheus metrics

deployments:
  - selector:
      - outlyer.com/type: 'host'
