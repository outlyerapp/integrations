---
title: SSL
description: Outlyer Monitoring Check the checks for the expiration of SSL certificates
icon:
  url: https://raw.githubusercontent.com/outlyerapp/integrations/master/ssl/resources/ssl.svg?sanitize=true
version: 1.0.0
author: 
  name: Outlyer
  url: https://www.outlyer.com
license: MIT
labels:
  web: ssl
form:
- name: selectors_ssl
  description: "This selector is used by the SSL check to monitor your SSL certificates by collecting their expiration time. Select an instance, e.g.: instance.hostname: <INSTANCE_HERE>"
  prompt: "Select an instance"
  type: labelSelectors
- name: ssl_host
  description: "The remote host where the certificate is installed, e.g.: outlyer.com"
  prompt: "The remote host where the certificate is installed"
  type: string
  required: True
