---
title: AWS DynamoDB
description: Outlyer Monitoring Integration to collect metrics from your AWS DynamoDB Tables
icon:
  url: https://raw.githubusercontent.com/outlyerapp/integrations/master/aws-dynamodb/resources/aws-dynamodb.svg?sanitize=true
version: 1.0.0
author:
  name: Outlyer
  url: https://www.outlyer.com
license: MIT
labels:
  aws: dynamodb
  data-store: dynamodb
form:
- name: selectors_dynamodb
  description: "This selector is used by the AWS DynamoDB check for querying the AWS CloudWatch API. Select an instance, e.g.: instance.hostname: <INSTANCE_HERE>"
  prompt: "Select an instance"
  type: labelSelectors
- name: dynamodb_aws_access_key
  description: "The AWS ACCESS KEY is used by the DynamoDB check for querying the AWS CloudWatch API"
  prompt: "Provide the AWS ACCESS KEY"
  type: string
  required: True
- name: dynamodb_aws_secret_key
  description: "The AWS SECRET KEY is used by the DynamoDB check for querying the AWS CloudWatch API"
  prompt: "Provide the AWS SECRET KEY"
  type: string
  required: True
- name: dynamodb_aws_region
  description: "The AWS REGION is used by the DynamoDB check for querying the AWS CloudWatch API"
  prompt: "Provide the AWS REGION"
  type: string
  required: True
