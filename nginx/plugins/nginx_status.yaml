---
type: "native"
requirements: ["requests==2.18.4"]
interval: 30

settings:
  - name: url
    type: string
    description: URL to the Nginx status page
    required: true

deployments:
  - selector:
      - outlyer.com/type: 'host'
    settings:
      url: http://localhost:8080/nginx_status
