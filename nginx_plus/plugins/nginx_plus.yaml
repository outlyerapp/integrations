---
type: "native"
requirements: ["requests==2.18.4"]
interval: 30

settings:
  - name: url
    type: string
    description: URL to the Nginx Plus status page
    required: true

deployments:
  - selector:
      - outlyer.com/type: 'host'
    url: https://demo.nginx.com/status
