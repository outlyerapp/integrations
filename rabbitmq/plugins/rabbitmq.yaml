---
type: "native"
requirements:
  - "requests==2.18.4"
deployments:
  - interval: 30
    selector:
      - outlyer.com/type: 'host'

    host: 'localhost'
    port: '15672'
    protocol: 'http'
    verify_ssl: True
    username: guest
    password: guest
